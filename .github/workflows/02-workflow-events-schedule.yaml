---
name: 02 - Workflow Events (schedule)

on:
  schedule:
    - cron: '0 0 * * *'

jobs:
  echo:
    runs-on: ubuntu-latest
    steps:
      - name: Show the trigger
        run: |
          echo "Event name: ${{ github.event_name }}"
